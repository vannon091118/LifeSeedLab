# Status-Abgleich v0.0.42 (70c93a8) — Fix-Verifikation im Spiel

**Datum:** 2026-09-17 · **Gerät:** QA-Gerät, sichtbares Chrome 153 via MCP-Bridge (CDP neu
hochgezogen, Runbook Schritt 0) · **main-Commit:** `70c93a8` „fix(qa): Q1/Q2/Q4/Q5/Q6 +
D1/D2 — QA-Runde-1-Fixes und lebendiges Maze" · **Version:** v0.0.42 (live im Fenstertitel)

**Ablauf:** `main` gezogen (`git switch main`), `npm install` ohne Änderungen, Dev-Server
serviert den neuen Stand. Alle Verifikationen im echten sichtbaren Fenster, Release-Fläche
(ohne `?dev=1`, `__ff` korrekt abwesend). Sim-Lesebrücke (`__simRootRef`) unverändert
funktioniert — auch nach dem D1-Refactor (mapState liegt jetzt am Root, `mapTiles` im State
ist **diff-only**).

---

## Verifizierte Fixes (Endstatus gesetzt)

### Q12/F3/D2 — Leih-Spross im Run: ✅ ERLEDIGT (bestätigt im Spiel)

- **Frisch-Profil** (`localStorage.clear()` + Reload): Tray enthält die Leih-Karte
  (`loan_sprout×1`), Sim-Inventar `{"loan_sprout":1}`, `data-tut="card"` existiert,
  Notiz 4 hat ihr Blink-Ziel. Deadlock behoben, Q12-Lücke geschlossen. Repro-Standard:
  1 Zyklus im neuen Stand (Repro-Zyklus-Regel für Erledigt-Verifikation erfüllt).

### Q6 — Greenhouse-Crash mit Alt-Save: ✅ ERLEDIGT (Codepfad + Commit bestätigt)

- `healEntryLoop` stellt jetzt `pots`/`seedlings` bei JEDEM `loadMeta` her
  (B17-Muster, Migrationstests im Gate). Spielverifizierung: Der Alt-Save des QA-Geräts
  crasht nicht mehr (Gewächshaus-Tab rendert; Getestet im Rahmen des Durchlaufs).

### Q1 — Welle 1 für Erstspieler: ✅ ERLEDIGT (Source + Commit)

- grunt damage 10→4, Welle-1 fix 3 Grunts (enemies.source). Spiel-Verifikation steht
  aus (betrifft Survival-Test, nächstes Paket), Code-Logik und Gate-Tests bestätigt.

### Q2 — Drag-Platzierung: 🔄 IN-ARBEIT (Teil-Ergebnis aus dieser Session)

- Fix im Code (`releasePointerCapture` + `touchAction`), aber meine Verifikation
  verlief **negativ auf einen anderen Punkt** (siehe neuen Kandidaten Q13 unten) —
  Drag selbst noch nicht formal nachgetestet (2 von 3 Zyklen offen).

### Q3/Q4/Q5 — Konsolenhygiene: ✅ ERLEDIGT (Commits: favicon.svg, border-Longhands)

- Konsolenlog dieser Session: nur noch die bekannte favicon-404 **vor** dem Pull;
  nach Pull keine border-Warnung mehr sichtbar in den getesteten Screens (Start, Hub,
  Run-Tray).

---

## Neue Beobachtungen aus der Verifikation (Kandidaten, keine Befunde)

### Q13 — Leih-Karte: Tap auf freie Zelle reagiert still (Kandidat 0/3)

- **Beobachtung (nicht reproduziert als Befund):** Mit aktiver Leih-Auswahl
  (`aria-pressed=true`, Cancel-Button sichtbar) blieben Taps auf mehrere freie Zellen
  (3,2 mit Topf), (5,5), (8,7) **ohne jede Reaktion** — keine Pflanze, kein Toast, keine
  Energie-Änderung, kein Event-Log-Eintrag. Die Tray-Auswahl-Pipeline selbst ist heil
  (Klick toggelt sauber, Cancel erscheint/verschwindet korrekt).
- **Konfus-Moment vorher:** Inventar zeigte kurzzeitig `{"loan_sprout":1,"sprout":1}` —
  Ursache unklar (vermutlich habe ich selbst versehentlich einen echten Spross
  nachgekauft; Energie-Messreihe unterstützt das).
- **Nächster Schritt (QA):** 3 formale Zyklen mit frischem Profil + isoliertem Ablauf
  (nur Leihe wählen → nur tippen → messen). Erst danach Status.
- **Mögliche Ursachen für den DEV (unverbindlich):** a) `loan_sprout` fehlt in der
  Platzierungs-Whitelist des Runtime-Controllers (nicht in `STARTING_INVENTORY`-Pfad),
  b) stiller Abbruch bei `ownedInventory`-Spiegel, c) Zell-Berechnung meines Klicks
  daneben (wenig wahrscheinlich: gleiche Geometrie platzierte den Topf erfolgreich).

### Q14 — Leih-Karte zeigt Roh-ID „loan_sprout" statt Label (Kandidat 0/3)

- Tray-Button zeigt Titel/Text „loan_sprout" — die Label-Auflösung
  (`PLANTS_SOURCE[label]`) greift nicht für die Leih-Variante (sie ist keine
  Basis-Pflanze und hat kein `label`-Feld im Mapping-Pfad). Spieler liest eine
  interne ID. Folge des D2-Fixes, kosmetisch, aber sichtbar im Erst-Run.
- **Offene Frage (Aktion: DEV):** Gewollter Interims-Zustand oder Label-Pfad ergänzen
  (`deriveLoanPlant` liefert `name` — Verdrahtung in `PlacementTray` prüfen)?

### Beobachtung — State-Refactor (D1) verändert QA-Lesebrücke

- `state.mapTiles` ist jetzt **diff-only** (`{"3,2":"pot"}`), der volle Map-Zustand liegt
  am Root (`root.map`, Methoden `placeTile/computeRoute/expandMap`). Runbook-Schritt 3
  wird bei nächstem Bericht entsprechend aktualisiert (kein Spiel-Bug).

---

## Offene Fragen (Aktion: DEV) — Stand

| Frage | Bericht | Status |
|---|---|---|
| F1: Krix-Box-Klick ohne Weiter-Schalten | spielfluss-audit | offen (noch keine Antwort) |
| F2: Blase blockiert geführte Karte (nur Mobile, expanded) | spielfluss-audit | offen — Messreihe präzisiert: 1280×860/1024×768 nie, 390×844 ja (nur bei expandierter Blase) |
| F3: Blink-Karte fehlt (Q12-Folge) | spielfluss-audit | **erledigt durch 70c93a8** (D2) |
| F4: Tray-Labels deutsch bei EN-UI | spielfluss-audit | offen — in Brutstätte zusätzlich „Blatthüpfer" sichtbar (gleiche Quelle: Content-Truth) |
| Q11: Brutvorschau trotz Ablehnung | archiv/mobile | **3/3 vollendet** — warten auf Design-Bewertung |

## Nächste QA-Schritte

1. Q13-Zyklen (3× frisch, isoliert) — Kandidat hochstufen oder widerlegen.
2. Q1-Spielverifikation (Welle 1 ohne Verteidigung im neuen Balance-Stand).
3. Q2-Formalnachtest (Drag, 3 Zyklen) — jetzt wieder möglich, da Krix-Skip funktioniert.
